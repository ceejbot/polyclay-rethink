language: node_js
node_js:
  - "0.12"
  - "0.10"
  - iojs
before_script:
  - sudo add-apt-repository ppa:rethinkdb/ppa -y
  - sudo apt-key update
  - sudo apt-get update -q
  - sudo apt-get install rethinkdb
script:
  - npm run test-travis
